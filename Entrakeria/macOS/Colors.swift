import SwiftUI

enum EntrakeiaPalette {
    static let bg = Color(red: 0.11, green: 0.11, blue: 0.12)      // dark charcoal
    static let card = Color(red: 0.15, green: 0.15, blue: 0.16)    // slightly lighter
    static let blue = Color(red: 0.42, green: 0.62, blue: 0.84)    // soft blue (highlight)
    static let text = Color(red: 0.95, green: 0.95, blue: 0.96)    // near white
    static let line = Color(red: 0.35, green: 0.35, blue: 0.37)    // muted gray
    static let accent = Color(red: 0.89, green: 0.73, blue: 0.35)  // warm gold
}
